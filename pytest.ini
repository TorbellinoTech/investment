[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short -m "not db"
markers =
    unit: Unit tests
    integration: Integration tests
    performance: Performance tests
    db: tests that require Postgres
